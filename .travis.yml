language: elixir

elixir:
  - 1.9.2
otp_release:
  - 22.1.4

env:
  - MIX_ENV=test

services:
  - postgresql

before_script:
  - cp config/travis.exs config/test.secret.exs
  - mix do ecto.create, ecto.migrate

script:
  - mix project.check
