language: elixir

elixir:
  - 1.11.2
otp_release:
  - 23.0.4

env:
  - MIX_ENV=test

services:
  - postgresql

before_script:
  - cp config/travis.exs config/test.secret.exs
  - mix do ecto.create, ecto.migrate

script:
  - mix project.check
